## What changed?

## Why?

## Tests performed

- [ ] `cargo fmt --all --check`
- [ ] `cargo check --workspace --all-targets`
- [ ] `cargo test --workspace`
- [ ] `cargo clippy --workspace --all-targets -- -D warnings`

## Related issue

Closes #

## Compatibility impact

Does this change the `--format json` schema, an exit code, a CLI flag, or
anything `ProtocolCanary-Action` or a fixture consumer depends on? If so,
note it and update `docs/json-report-contract.md` / `docs/fixture-contract.md`
accordingly.

## Breaking change?

- [ ] Yes — described above
- [ ] No
