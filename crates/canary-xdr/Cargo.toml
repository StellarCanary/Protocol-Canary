[package]
name = "canary-xdr"
description = "XDR compatibility runner for Stellar Protocol Canary."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints.clippy]
all = "warn"

[dependencies]
