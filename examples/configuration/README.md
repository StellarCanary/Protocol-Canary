# Configuration example

A fully annotated `.stellar-canary.toml` showing every field this build
understands, with comments explaining what each one does. Copy it as a
starting point and delete the comments you don't need.

Run it the same way as the [minimal example](../minimal/README.md):

```bash
cargo run -p canary-cli --manifest-path ../../Cargo.toml -- check
```
